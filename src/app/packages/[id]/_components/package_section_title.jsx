const PackageSectionTitle = ({ title }) => {
  return (
    <h1 className="text-[30px] font-inter font-bold text-tourHub-green-dark leading-[45px]">
      {title}
    </h1>
  );
};

export default PackageSectionTitle;
